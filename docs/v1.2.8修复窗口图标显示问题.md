# v1.2.8 修复窗口图标显示问题

## 修复内容

修复了应用窗口标题栏和任务栏中图标显示不正确的问题。

## 问题描述

在开发模式和打包后的应用中，窗口标题栏和任务栏显示的是通用文档图标（📄），而不是自定义的应用图标。

## 修复方案

### 1. 优化图标路径逻辑

修改了 `src/main/main.ts` 中的图标路径计算逻辑：

- **开发模式**：从项目根目录的 `build` 文件夹读取图标
- **生产模式**：从打包后的 `resources` 目录读取图标
- **平台适配**：
  - Windows: 使用 `.ico` 格式
  - macOS: 使用 `.icns` 格式
  - Linux: 使用 `.png` 格式

### 2. 添加窗口标题

显式设置窗口标题为"日报助手"，确保应用名称正确显示。

### 3. 添加调试日志

添加了图标路径和文件存在性的日志输出，便于排查问题。

## 技术细节

```typescript
let iconPath: string;
if (process.env.NODE_ENV === 'development') {
  // 开发模式路径
  iconPath = process.platform === 'win32'
    ? path.join(__dirname, '../../build/icon.ico')
    : process.platform === 'darwin'
    ? path.join(__dirname, '../../build/icon.icns')
    : path.join(__dirname, '../../build/icon.png');
} else {
  // 生产模式路径
  iconPath = process.platform === 'win32'
    ? path.join(process.resourcesPath, 'build/icon.ico')
    : process.platform === 'darwin'
    ? path.join(process.resourcesPath, 'build/icon.icns')
    : path.join(process.resourcesPath, 'build/icon.png');
}
```

## 影响范围

- 窗口标题栏图标显示
- 任务栏/Dock 图标显示
- 应用切换器中的图标显示

## 测试建议

1. 开发模式测试：`npm run dev:electron`
2. 打包后测试：
   - Windows: `npm run package:win`
   - macOS: `npm run package:mac`
   - Linux: `npm run package:linux`

## 版本信息

- 版本号：v1.2.8
- 发布日期：2026-02-24
