# 优化今日已提交内容显示逻辑

## 问题描述

在日报助手中，"今日已提交内容"显示存在跨日问题：
- 第二天启动应用时，仍然显示昨天的内容
- 用户期望今天没有内容时显示为空
- 缺少跨日刷新机制

## 解决方案

### 1. 后端优化：严格按日期过滤

#### 新增 `filterTodayContent()` 方法

```typescript
private filterTodayContent(content: string, todayDateString: string): string {
  if (!content) return '';
  
  const lines = content.split('\n');
  const todayLines: string[] = [];
  let isInTodaySection = false;
  let foundTodayHeader = false;
  
  for (const line of lines) {
    // 检查是否是今天的日期标题 (格式: # 2026/3/10 日报)
    if (line.match(/^# \d{4}\/\d{1,2}\/\d{1,2}/) && line.includes(todayDateString)) {
      isInTodaySection = true;
      foundTodayHeader = true;
      todayLines.push(line);
      continue;
    }
    
    // 检查是否遇到其他日期标题（结束今天的内容）
    if (line.match(/^# \d{4}\/\d{1,2}\/\d{1,2}/) && !line.includes(todayDateString)) {
      isInTodaySection = false;
      continue;
    }
    
    // 如果在今天的内容区域，添加到结果
    if (isInTodaySection) {
      todayLines.push(line);
    }
  }
  
  // 如果没有找到今天的标题，返回空字符串
  if (!foundTodayHeader) {
    return '';
  }
  
  return todayLines.join('\n').trim();
}
```

#### 优化 `getTodayReport()` 方法

```typescript
async getTodayReport(): Promise<string> {
  const today = new Date();
  const todayDateString = today.toLocaleDateString('zh-CN');
  const filePath = this.generateFilePath(today);
  
  // 1. 先检查本地缓存
  const cachedReport = this.store.get('cachedReport');
  if (cachedReport && cachedReport.filePath === filePath) {
    return this.filterTodayContent(cachedReport.content, todayDateString);
  }
  
  // 2. 从 GitHub 获取
  try {
    const { data } = await this.octokit.repos.getContent({
      owner: this.config.repoOwner,
      repo: this.config.repoName,
      path: filePath,
      ref: this.config.branch,
    });
    
    if ('content' in data) {
      const content = Buffer.from(data.content, 'base64').toString();
      return this.filterTodayContent(content, todayDateString);
    }
    return '';
  } catch (error) {
    return '';
  }
}
```

### 2. 前端优化：添加跨日检测和定时刷新

#### 启动时跨日检测

```typescript
useEffect(() => {
  // 启动时检查是否需要跨日刷新
  const initializeToday = () => {
    const now = new Date();
    const lastRefresh = localStorage.getItem('lastTodayReportRefresh');
    const lastRefreshDate = lastRefresh ? new Date(lastRefresh) : null;
    
    // 如果是新的一天，清空显示并重新加载
    if (!lastRefreshDate || 
        now.toDateString() !== lastRefreshDate.toDateString()) {
      console.log('检测到新的一天，刷新今日内容');
      setTodayReport(''); // 先清空显示
      loadTodayReport();
      localStorage.setItem('lastTodayReportRefresh', now.toISOString());
    } else {
      // 同一天，正常加载
      loadTodayReport();
    }
  };
  
  initializeToday();
  // ...
}, []);
```

#### 定时跨日检测

```typescript
// 每小时检查一次是否需要跨日刷新
const refreshInterval = setInterval(() => {
  const now = new Date();
  const lastRefresh = localStorage.getItem('lastTodayReportRefresh');
  const lastRefreshDate = lastRefresh ? new Date(lastRefresh) : null;
  
  // 如果是新的一天，刷新今日内容
  if (!lastRefreshDate || 
      now.toDateString() !== lastRefreshDate.toDateString()) {
    console.log('定时检测到新的一天，刷新今日内容');
    setTodayReport(''); // 先清空显示
    loadTodayReport();
    localStorage.setItem('lastTodayReportRefresh', now.toISOString());
  }
}, 60 * 60 * 1000); // 每小时检查一次
```

#### 优化内容加载逻辑

```typescript
const loadTodayReport = async () => {
  try {
    const result = await window.electronAPI.getTodayReport();
    if (result.success) {
      // 即使内容为空也要设置，这样可以清空之前的显示
      setTodayReport(result.content || '');
    } else {
      setTodayReport('');
    }
  } catch (error) {
    console.error('Failed to load today report:', error);
    setTodayReport('');
  }
};
```

#### 优化 UI 显示

```typescript
{/* Today's Report Preview */}
<div className="mx-6 mt-4 p-4 bg-blue-50 border border-blue-200 rounded-lg">
  <div className="text-sm text-blue-800 font-medium mb-2">今日已提交内容：</div>
  {todayReport ? (
    <div className="text-sm text-blue-700 whitespace-pre-wrap max-h-32 overflow-y-auto">
      {todayReport}
    </div>
  ) : (
    <div className="text-sm text-gray-500 italic">
      今天还没有提交内容
    </div>
  )}
</div>
```

## 实现细节

### 日期过滤算法

1. **按行分割内容**：将完整内容按换行符分割
2. **识别日期标题**：使用正则表达式匹配 `# YYYY/M/D 日报` 格式
3. **区域标记**：标记是否在今天的内容区域内
4. **内容收集**：只收集今天区域内的内容
5. **结果返回**：如果没有找到今天的标题，返回空字符串

### 跨日检测机制

1. **localStorage 存储**：记录最后刷新时间
2. **日期比较**：使用 `toDateString()` 比较日期
3. **自动刷新**：检测到新的一天时自动刷新
4. **定时检查**：每小时检查一次，确保长时间运行的应用也能正确刷新

### 刷新策略

- **启动时检查**：应用启动时立即检查是否需要跨日刷新
- **定时检查**：每小时检查一次，适合长时间运行的场景
- **先清空后加载**：检测到新的一天时，先清空显示再加载新内容

## 代码变更

### 修改文件

1. `src/main/github-service.ts`
   - 新增 `filterTodayContent()` 方法
   - 优化 `getTodayReport()` 方法

2. `src/renderer/components/MainEditor.tsx`
   - 优化 `useEffect` 初始化逻辑
   - 修改 `loadTodayReport()` 方法
   - 优化今日内容显示 UI

### 具体变更

#### 后端变更

```diff
// src/main/github-service.ts

async getTodayReport(): Promise<string> {
+ const today = new Date();
+ const todayDateString = today.toLocaleDateString('zh-CN');
  const filePath = this.generateFilePath(new Date());
  
+ // 1. 先检查本地缓存
+ const cachedReport = this.store.get('cachedReport');
+ if (cachedReport && cachedReport.filePath === filePath) {
+   return this.filterTodayContent(cachedReport.content, todayDateString);
+ }
+ 
+ // 2. 从 GitHub 获取
  try {
    const { data } = await this.octokit.repos.getContent({
      owner: this.config.repoOwner,
      repo: this.config.repoName,
      path: filePath,
      ref: this.config.branch,
    });
    
    if ('content' in data) {
-     return Buffer.from(data.content, 'base64').toString();
+     const content = Buffer.from(data.content, 'base64').toString();
+     return this.filterTodayContent(content, todayDateString);
    }
    return '';
  } catch (error) {
    return '';
  }
}

+ /**
+  * 过滤今天的内容
+  */
+ private filterTodayContent(content: string, todayDateString: string): string {
+   // ... 实现逻辑
+ }
```

#### 前端变更

```diff
// src/renderer/components/MainEditor.tsx

useEffect(() => {
+ // 启动时检查是否需要跨日刷新
+ const initializeToday = () => {
+   const now = new Date();
+   const lastRefresh = localStorage.getItem('lastTodayReportRefresh');
+   const lastRefreshDate = lastRefresh ? new Date(lastRefresh) : null;
+   
+   if (!lastRefreshDate || 
+       now.toDateString() !== lastRefreshDate.toDateString()) {
+     setTodayReport('');
+     loadTodayReport();
+     localStorage.setItem('lastTodayReportRefresh', now.toISOString());
+   } else {
+     loadTodayReport();
+   }
+ };
+ 
+ initializeToday();
  loadCommitStatus();
  
- const interval = setInterval(loadCommitStatus, 60000);
+ const statusInterval = setInterval(loadCommitStatus, 60000);
+ 
+ // 每小时检查一次是否需要跨日刷新
+ const refreshInterval = setInterval(() => {
+   // ... 跨日检测逻辑
+ }, 60 * 60 * 1000);
+ 
  return () => {
-   clearInterval(interval);
+   clearInterval(statusInterval);
+   clearInterval(refreshInterval);
  };
}, []);

const loadTodayReport = async () => {
  try {
    const result = await window.electronAPI.getTodayReport();
-   if (result.success && result.content) {
-     setTodayReport(result.content);
-   }
+   if (result.success) {
+     setTodayReport(result.content || '');
+   } else {
+     setTodayReport('');
+   }
  } catch (error) {
    console.error('Failed to load today report:', error);
+   setTodayReport('');
  }
};
```

## 测试场景

### 场景 1：跨日启动测试 ✅
```
操作：昨天提交日报，今天启动应用
期望：今日内容显示"今天还没有提交内容"
验证：localStorage 记录更新，todayReport 为空
```

### 场景 2：同日重启测试 ✅
```
操作：今天提交日报，重启应用
期望：正常显示今天的内容
验证：todayReport 包含今天的内容
```

### 场景 3：跨日运行测试 ✅
```
操作：应用昨天启动并一直运行到今天
期望：今天 0 点后自动刷新显示
验证：定时器触发，内容自动清空
```

### 场景 4：本地缓存测试 ✅
```
操作：今天有本地缓存（未推送），重启应用
期望：显示本地缓存的今日内容
验证：从缓存中正确过滤今日内容
```

## 构建验证

```bash
npm run build:main
# ✅ 构建成功

npm run build:renderer  
# ✅ 构建成功
```

## 性能影响

### 正面影响
- ✅ 内容显示更准确
- ✅ 用户体验更一致
- ✅ 自动跨日刷新

### 性能考虑
- 🟡 增加了定时器（每小时检查一次）
- 🟡 增加了 localStorage 读写
- 🟢 内容过滤算法简单高效

### 内存占用
- 增加很少，主要是定时器和少量状态

## 用户体验改进

### 1. 视觉反馈
- 新的一天时显示"今天还没有提交内容"
- 避免显示过期内容造成的困惑

### 2. 自动化
- 无需用户手动刷新
- 启动时自动检测
- 长时间运行时自动跨日刷新

### 3. 一致性
- 今日内容严格按日期过滤
- 本地缓存和远程内容处理一致

## 后续优化建议

### 短期优化
1. 添加手动刷新按钮
2. 优化跨时区支持
3. 添加内容变更动画

### 中期优化
1. 支持自定义刷新间隔
2. 添加内容变更通知
3. 优化大文件的过滤性能

### 长期优化
1. 支持多日内容预览
2. 添加内容搜索功能
3. 支持内容导出

## 版本信息

- **功能类型**：优化
- **影响范围**：今日内容显示逻辑
- **向后兼容**：是
- **计划版本**：v1.1.0（Unreleased）
- **完成日期**：2026-03-10

## 总结

通过添加严格的日期过滤和跨日检测机制，成功解决了"今日已提交内容"显示过期内容的问题。现在应用能够：

1. **准确显示**：只显示今天的内容，过滤掉其他日期的内容
2. **自动刷新**：启动时和定时检查时自动跨日刷新
3. **用户友好**：没有内容时显示清晰的提示信息
4. **性能优化**：高效的内容过滤算法，最小的性能影响

这个优化显著提升了用户体验，确保了内容显示的准确性和时效性。