# 打包指南

## 📦 打包方式

### 方式一：本地打包（推荐用于开发测试）

#### 在 macOS 上
```bash
npm run package:mac
```
生成：`release/DailyReport-1.0.0.dmg`

#### 在 Windows 上
```bash
npm run package:win
```
生成：`release/DailyReport Setup 1.0.0.exe`

#### 在 Linux 上
```bash
npm run package:linux
```
生成：
- `release/DailyReport-1.0.0.AppImage`
- `release/daily-report-app_1.0.0_amd64.deb`

### 方式二：GitHub Actions 自动打包（推荐用于发布）

#### 设置步骤

1. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "准备发布"
   git push
   ```

2. **创建版本标签**
   ```bash
   git tag v1.0.0
   git push origin v1.0.0
   ```

3. **自动构建**
   - GitHub Actions 会自动在 macOS、Windows、Linux 上构建
   - 构建完成后会自动创建 Release
   - 所有平台的安装包都会上传到 Release

4. **下载安装包**
   - 访问 GitHub 仓库的 Releases 页面
   - 下载对应平台的安装包

#### 手动触发构建

也可以在 GitHub 仓库的 Actions 页面手动触发构建：
1. 进入 Actions 标签
2. 选择 "构建和发布" workflow
3. 点击 "Run workflow"

## 🔧 打包前准备

### 1. 确保代码已提交
```bash
git status
git add .
git commit -m "准备打包"
```

### 2. 更新版本号
编辑 `package.json`：
```json
{
  "version": "1.0.0"
}
```

### 3. 测试构建
```bash
npm run build
```
确保没有错误。

## 📂 打包产物说明

### macOS
- **DMG 镜像**：`DailyReport-1.0.0.dmg`
  - 双击打开，拖拽到 Applications 文件夹
  - 大小约 100-150 MB

### Windows
- **NSIS 安装程序**：`DailyReport Setup 1.0.0.exe`
  - 双击运行安装向导
  - 默认安装到 `C:\Program Files\DailyReport`
  - 大小约 80-120 MB

### Linux
- **AppImage**：`DailyReport-1.0.0.AppImage`
  - 添加执行权限：`chmod +x DailyReport-1.0.0.AppImage`
  - 直接运行：`./DailyReport-1.0.0.AppImage`
  - 无需安装，便携式
  
- **DEB 包**：`daily-report-app_1.0.0_amd64.deb`
  - 适用于 Debian/Ubuntu
  - 安装：`sudo dpkg -i daily-report-app_1.0.0_amd64.deb`

## ⚠️ 常见问题

### 问题 1：在 Linux 上打包 Windows 版本失败

**错误**：`wine is required`

**原因**：在 Linux 上打包 Windows 应用需要 Wine

**解决方案**：
1. 使用 GitHub Actions 自动打包（推荐）
2. 或在 Windows 电脑上打包
3. 或安装 Wine（不推荐，配置复杂）

### 问题 2：打包后文件很大

**原因**：Electron 应用包含完整的 Chromium 和 Node.js

**说明**：
- macOS DMG：约 100-150 MB
- Windows EXE：约 80-120 MB
- Linux AppImage：约 100-140 MB

这是正常的，无法显著减小。

### 问题 3：macOS 提示"无法验证开发者"

**解决方案**：
1. 打开"系统偏好设置" → "安全性与隐私"
2. 点击"仍要打开"

**生产环境**：需要 Apple Developer 账号进行代码签名。

### 问题 4：Windows 杀毒软件报警

**原因**：未签名的应用可能被误报

**解决方案**：
1. 开发测试：添加信任
2. 生产环境：购买代码签名证书

## 🚀 发布流程

### 开发版本（测试）
```bash
# 1. 本地打包
npm run package:mac  # 或其他平台

# 2. 测试安装包
# 安装并测试所有功能

# 3. 修复问题后重新打包
```

### 正式版本（发布）
```bash
# 1. 更新版本号
# 编辑 package.json 中的 version

# 2. 提交代码
git add .
git commit -m "Release v1.0.0"
git push

# 3. 创建标签
git tag v1.0.0
git push origin v1.0.0

# 4. GitHub Actions 自动构建
# 等待构建完成（约 10-15 分钟）

# 5. 检查 Release
# 访问 GitHub Releases 页面
# 下载并测试所有平台的安装包

# 6. 发布
# 编辑 Release 说明
# 取消 Draft 状态
```

## 📝 版本号规范

遵循语义化版本（Semantic Versioning）：

- **主版本号**：不兼容的 API 修改
- **次版本号**：向下兼容的功能性新增
- **修订号**：向下兼容的问题修正

示例：
- `1.0.0` - 首次正式发布
- `1.0.1` - 修复 Bug
- `1.1.0` - 新增功能
- `2.0.0` - 重大更新

## 🔐 代码签名（可选）

### macOS
需要：
- Apple Developer 账号（$99/年）
- 开发者证书

配置：
```json
"mac": {
  "identity": "Developer ID Application: Your Name (TEAM_ID)"
}
```

### Windows
需要：
- 代码签名证书（约 $100-300/年）

配置：
```json
"win": {
  "certificateFile": "path/to/cert.pfx",
  "certificatePassword": "password"
}
```

## 💡 最佳实践

1. **使用 GitHub Actions**：自动化打包，省时省力
2. **版本管理**：每次发布都打标签
3. **测试充分**：在所有目标平台上测试
4. **更新日志**：在 Release 中说明更新内容
5. **备份安装包**：保存历史版本的安装包

## 📊 打包时间参考

- **本地打包**：2-5 分钟
- **GitHub Actions**：10-15 分钟（三个平台并行）

## 🎯 快速命令

```bash
# 本地打包
npm run package:mac      # macOS
npm run package:win      # Windows
npm run package:linux    # Linux

# 发布到 GitHub
git tag v1.0.0
git push origin v1.0.0

# 查看打包结果
ls -lh release/
```
