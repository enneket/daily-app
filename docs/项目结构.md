# 项目结构

## 📂 目录结构

```
.
├── .github/                    # GitHub 配置
│   └── workflows/
│       └── build.yml          # 自动打包 workflow
│
├── docs/                       # 📚 文档目录
│   ├── README.md              # 文档索引
│   ├── 从这里开始.md          # 快速入门
│   ├── 快速开始.md            # 详细安装指南
│   ├── 使用说明.md            # 开发指南
│   ├── 日报桌面应用.md        # 技术文档
│   ├── 打包快速参考.md        # 打包命令
│   ├── 打包指南.md            # 打包详细说明
│   ├── 打包方案总结.md        # 方案对比
│   ├── 当前环境打包说明.md    # Linux 环境方案
│   ├── 平台说明.md            # 平台特定问题
│   ├── 项目状态.md            # 项目状态
│   └── 项目结构.md            # 本文件
│
├── plans/                      # 📋 计划目录
│   └── 2026-02-11-日报桌面应用.md
│
├── src/                        # 💻 源代码
│   ├── main/                  # Electron 主进程
│   │   ├── main.ts           # 主进程入口
│   │   ├── preload.ts        # 预加载脚本
│   │   └── github-service.ts # GitHub API 服务
│   │
│   └── renderer/              # React 渲染进程
│       ├── components/
│       │   ├── MainEditor.tsx # 主编辑器组件
│       │   └── Settings.tsx   # 设置页面组件
│       ├── App.tsx            # 应用根组件
│       ├── main.tsx           # 渲染进程入口
│       ├── types.ts           # TypeScript 类型定义
│       └── index.css          # 全局样式
│
├── dist/                       # 🔨 构建输出（自动生成）
│   ├── main/                  # 编译后的主进程代码
│   └── renderer/              # 编译后的渲染进程代码
│
├── release/                    # 📦 打包输出（自动生成）
│   ├── DailyReport-1.0.0.dmg          # macOS 安装包
│   ├── DailyReport Setup 1.0.0.exe    # Windows 安装程序
│   └── DailyReport-1.0.0.AppImage     # Linux 便携版
│
├── node_modules/               # 📦 依赖包（自动生成）
│
├── .gitignore                  # Git 忽略配置
├── .npmrc                      # npm 配置
├── electron.js                 # Electron 启动脚本
├── index.html                  # HTML 入口
├── package.json                # 项目配置和依赖
├── package-lock.json           # 依赖锁定文件
├── postcss.config.js           # PostCSS 配置
├── tailwind.config.js          # Tailwind CSS 配置
├── tsconfig.json               # TypeScript 配置（渲染进程）
├── tsconfig.main.json          # TypeScript 配置（主进程）
├── tsconfig.node.json          # TypeScript 配置（Node.js）
├── vite.config.ts              # Vite 构建配置
└── README.md                   # 项目说明
```

## 📝 文件说明

### 配置文件

| 文件 | 说明 |
|------|------|
| `package.json` | 项目配置、依赖、脚本命令 |
| `tsconfig.json` | 渲染进程 TypeScript 配置 |
| `tsconfig.main.json` | 主进程 TypeScript 配置 |
| `vite.config.ts` | Vite 构建工具配置 |
| `tailwind.config.js` | Tailwind CSS 配置 |
| `postcss.config.js` | PostCSS 配置 |
| `.npmrc` | npm 镜像配置 |
| `.gitignore` | Git 忽略规则 |

### 源代码

#### 主进程（src/main/）

| 文件 | 说明 |
|------|------|
| `main.ts` | Electron 主进程入口，创建窗口、处理 IPC |
| `preload.ts` | 预加载脚本，暴露安全的 API 给渲染进程 |
| `github-service.ts` | GitHub API 封装，处理日报提交 |

#### 渲染进程（src/renderer/）

| 文件 | 说明 |
|------|------|
| `main.tsx` | React 应用入口 |
| `App.tsx` | 应用根组件，路由和状态管理 |
| `types.ts` | TypeScript 类型定义 |
| `index.css` | 全局样式和 Tailwind 导入 |
| `components/MainEditor.tsx` | 主编辑器界面 |
| `components/Settings.tsx` | 设置页面 |

### 文档

| 文件 | 说明 |
|------|------|
| `README.md` | 项目介绍和快速开始 |
| `docs/README.md` | 文档索引 |
| `docs/从这里开始.md` | 3 步快速入门 |
| `docs/快速开始.md` | 详细安装指南 |
| `docs/使用说明.md` | 开发和调试指南 |
| `docs/日报桌面应用.md` | 技术文档和架构 |
| `docs/打包指南.md` | 打包和发布流程 |
| `docs/平台说明.md` | 平台特定问题 |
| `docs/项目状态.md` | 项目进度和状态 |

### 自动生成的目录

| 目录 | 说明 | 是否提交 |
|------|------|----------|
| `dist/` | 构建输出 | ❌ 不提交 |
| `release/` | 打包输出 | ❌ 不提交 |
| `node_modules/` | 依赖包 | ❌ 不提交 |

## 🔄 工作流程

### 开发流程

```
编辑 src/renderer/ 
    ↓
Vite 自动编译
    ↓
浏览器/Electron 自动刷新
```

### 构建流程

```
npm run build
    ↓
编译 TypeScript (src/main/) → dist/main/
    ↓
构建 React (src/renderer/) → dist/renderer/
```

### 打包流程

```
npm run package:mac/win/linux
    ↓
运行 npm run build
    ↓
electron-builder 打包
    ↓
生成安装包 → release/
```

## 📊 代码统计

### 源代码文件

- **主进程**: 3 个文件
- **渲染进程**: 6 个文件
- **配置文件**: 8 个文件
- **文档**: 12 个文件

### 代码行数（估算）

- **TypeScript**: ~800 行
- **React/TSX**: ~600 行
- **配置**: ~200 行
- **文档**: ~2000 行

## 🎯 关键文件

### 必须了解

1. `src/main/main.ts` - Electron 主进程逻辑
2. `src/renderer/App.tsx` - React 应用入口
3. `package.json` - 项目配置和脚本

### 经常修改

1. `src/renderer/components/MainEditor.tsx` - 主界面
2. `src/renderer/components/Settings.tsx` - 设置页面
3. `src/main/github-service.ts` - GitHub API 逻辑

### 很少修改

1. `vite.config.ts` - 构建配置
2. `tsconfig.*.json` - TypeScript 配置
3. `electron.js` - 启动脚本

## 💡 最佳实践

### 添加新功能

1. 在 `src/renderer/components/` 创建新组件
2. 在 `src/main/` 添加主进程逻辑（如果需要）
3. 在 `src/renderer/types.ts` 添加类型定义
4. 更新文档

### 修改样式

1. 编辑组件中的 Tailwind 类名
2. 或在 `src/renderer/index.css` 添加全局样式
3. 或在 `tailwind.config.js` 自定义主题

### 添加依赖

```bash
# 运行时依赖
npm install package-name

# 开发依赖
npm install -D package-name
```

### 更新文档

1. 在 `docs/` 目录添加或修改文档
2. 更新 `docs/README.md` 索引
3. 更新根目录 `README.md` 链接

## 🔍 查找文件

### 按功能查找

- **UI 组件**: `src/renderer/components/`
- **业务逻辑**: `src/main/`
- **类型定义**: `src/renderer/types.ts`
- **样式**: `src/renderer/index.css`
- **配置**: 根目录 `*.config.*` 文件
- **文档**: `docs/` 目录

### 按文件类型查找

- **TypeScript**: `src/**/*.ts`
- **React**: `src/**/*.tsx`
- **配置**: `*.config.{js,ts}`
- **文档**: `docs/**/*.md`

## 📚 相关文档

- [使用说明.md](./使用说明.md) - 开发流程
- [日报桌面应用.md](./日报桌面应用.md) - 技术架构
- [项目状态.md](./项目状态.md) - 项目进度
