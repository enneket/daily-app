# 修复一次提交出现两个记录问题

## 问题描述

用户反馈在提交日报时，一次提交出现了两个记录，导致内容重复。

## 问题分析

### 可能的原因

1. **重复调用问题**：`submitReport()` 方法被重复调用
2. **缓存逻辑问题**：本地缓存和远程内容合并时出现重复
3. **时间戳重复**：同一时间戳下的多次提交
4. **UI 双击问题**：用户快速双击提交按钮
5. **异步竞态条件**：多个异步操作同时进行

### 当前实现分析

让我检查 `submitReport()` 方法的实现：

```typescript
async submitReport(content: string): Promise<void> {
  // 1. 生成文件路径和时间戳
  const filePath = this.generateFilePath(new Date());
  const timestamp = new Date().toLocaleTimeString('zh-CN', { 
    hour: '2-digit', 
    minute: '2-digit' 
  });
  
  // 2. 获取现有内容
  let existingContent = '';
  let sha: string | undefined;
  
  // 先尝试从缓存获取
  const cachedReport = this.store.get('cachedReport');
  if (cachedReport && cachedReport.filePath === filePath) {
    existingContent = cachedReport.content;
    sha = cachedReport.sha;
  } else {
    // 从 GitHub 获取现有内容
    // ...
  }
  
  // 3. 追加新内容
  const newContent = `${existingContent}\n## ${timestamp}\n${content}\n`;
  
  // 4. 保存到本地缓存
  this.store.set('cachedReport', {
    filePath,
    content: newContent,
    sha
  });
  
  // 5. 增加待提交计数
  const pendingCommits = (this.store.get('pendingCommits') || 0) + 1;
  this.store.set('pendingCommits', pendingCommits);
  
  // 6. 检查是否需要自动提交到 GitHub
  if (this.shouldAutoPush()) {
    await this.pushToGitHub(filePath, newContent, sha);
    // ...
  }
}
```

### 潜在问题点

1. **时间戳精度不够**：如果在同一分钟内多次提交，时间戳相同
2. **缓存更新时机**：缓存更新和计数增加之间可能存在竞态条件
3. **UI 防抖缺失**：前端没有防止快速重复点击
4. **异步操作重叠**：多个 `submitReport` 调用可能同时进行

## 解决方案

### 1. 前端防抖机制

```typescript
// MainEditor.tsx
const [submitting, setSubmitting] = useState(false);

const handleSubmit = async () => {
  if (!content.trim()) {
    showMessage('error', '请输入日报内容');
    return;
  }

  // 防止重复提交
  if (submitting) {
    return;
  }

  setSubmitting(true);
  try {
    const result = await window.electronAPI.submitReport(content);
    // ...
  } finally {
    setSubmitting(false);
  }
};
```

### 2. 提高时间戳精度

```typescript
// github-service.ts
const timestamp = new Date().toLocaleTimeString('zh-CN', { 
  hour: '2-digit', 
  minute: '2-digit',
  second: '2-digit' // 添加秒数
});

// 或者使用更精确的时间戳
const timestamp = new Date().toISOString().slice(11, 19); // HH:MM:SS
```

### 3. 添加内容去重检查

```typescript
// github-service.ts
private isDuplicateContent(existingContent: string, newContent: string, timestamp: string): boolean {
  // 检查是否已经存在相同时间戳的内容
  const lines = existingContent.split('\n');
  const timestampPattern = `## ${timestamp}`;
  
  return lines.some(line => line.includes(timestampPattern));
}

async submitReport(content: string): Promise<void> {
  // ... 现有逻辑
  
  // 检查是否重复
  if (this.isDuplicateContent(existingContent, content, timestamp)) {
    console.log('检测到重复内容，跳过提交');
    return;
  }
  
  // ... 继续处理
}
```

### 4. 添加提交锁机制

```typescript
// github-service.ts
private isSubmitting = false;

async submitReport(content: string): Promise<void> {
  // 防止并发提交
  if (this.isSubmitting) {
    console.log('正在提交中，跳过重复请求');
    return;
  }
  
  this.isSubmitting = true;
  
  try {
    // ... 现有逻辑
  } finally {
    this.isSubmitting = false;
  }
}
```

### 5. 优化缓存更新逻辑

```typescript
// github-service.ts
async submitReport(content: string): Promise<void> {
  const filePath = this.generateFilePath(new Date());
  const timestamp = new Date().toISOString().slice(11, 19); // 精确到秒
  
  // 防止并发提交
  if (this.isSubmitting) {
    throw new Error('正在提交中，请稍后再试');
  }
  
  this.isSubmitting = true;
  
  try {
    let existingContent = '';
    let sha: string | undefined;
    
    // 获取现有内容
    const cachedReport = this.store.get('cachedReport');
    if (cachedReport && cachedReport.filePath === filePath) {
      existingContent = cachedReport.content;
      sha = cachedReport.sha;
    } else {
      // 从 GitHub 获取
      // ...
    }
    
    // 检查重复
    if (this.isDuplicateContent(existingContent, content, timestamp)) {
      console.log('检测到重复内容，跳过提交');
      return;
    }
    
    // 追加新内容
    const newContent = `${existingContent}\n## ${timestamp}\n${content}\n`;
    
    // 原子性更新缓存和计数
    const pendingCommits = (this.store.get('pendingCommits') || 0) + 1;
    this.store.set('cachedReport', {
      filePath,
      content: newContent,
      sha
    });
    this.store.set('pendingCommits', pendingCommits);
    
    console.log(`日报已保存到本地，当前待提交数量: ${pendingCommits}`);
    
    // 检查是否需要自动提交到 GitHub
    if (this.shouldAutoPush()) {
      await this.pushToGitHub(filePath, newContent, sha);
      
      // 清除缓存和计数
      this.store.delete('cachedReport');
      this.store.set('pendingCommits', 0);
      this.store.set('lastPushTime', Date.now());
      console.log('已自动提交到 GitHub');
    }
  } finally {
    this.isSubmitting = false;
  }
}
```

## 实现方案

### 1. 后端修改

#### 文件：`src/main/github-service.ts`

**新增属性**：
```typescript
private isSubmitting = false;
```

**新增方法**：
```typescript
private isDuplicateContent(existingContent: string, newContent: string, timestamp: string): boolean
```

**修改方法**：
```typescript
async submitReport(content: string): Promise<void>
```

### 2. 前端修改

#### 文件：`src/renderer/components/MainEditor.tsx`

**优化防抖**：
- 确保 `submitting` 状态正确管理
- 添加更严格的重复提交检查

## 测试场景

### 场景 1：快速双击测试
1. 用户快速双击提交按钮
2. **期望**：只提交一次
3. **验证**：只有一个时间戳记录

### 场景 2：同一分钟多次提交
1. 用户在同一分钟内提交多次
2. **期望**：每次都有不同的时间戳
3. **验证**：时间戳精确到秒

### 场景 3：网络延迟测试
1. 网络较慢时用户多次点击
2. **期望**：只处理第一次点击
3. **验证**：后续点击被忽略

### 场景 4：并发提交测试
1. 模拟多个并发提交请求
2. **期望**：只有一个成功
3. **验证**：其他请求被拒绝

## 风险评估

### 技术风险
- **锁机制**：需要确保锁能正确释放
- **时间戳格式**：需要保持与现有格式兼容
- **用户体验**：防抖可能影响响应性

### 缓解措施
- 使用 try-finally 确保锁释放
- 渐进式升级时间戳格式
- 合理的防抖时间（不影响正常使用）

## 验收标准

- ✅ 快速双击不会产生重复记录
- ✅ 同一分钟内多次提交有不同时间戳
- ✅ 并发提交只有一个成功
- ✅ 网络延迟时不会重复提交
- ✅ 不影响正常的提交功能
- ✅ 构建无错误

## 时间估算

- 后端实现：1.5 小时
- 前端优化：0.5 小时
- 测试验证：1 小时
- 文档编写：0.5 小时
- **总计**：3.5 小时