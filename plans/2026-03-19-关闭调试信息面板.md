# 关闭调试信息面板

## 问题描述
MainEditor.tsx 中存在调试信息面板，在生产环境中不应该显示给用户。

## 解决方案
移除 MainEditor.tsx 中的：
1. `debugInfo` 状态
2. `addDebugInfo` 方法
3. 所有 `addDebugInfo(...)` 调用
4. 调试信息面板的 JSX 渲染部分

## 涉及文件
- `src/renderer/components/MainEditor.tsx`
