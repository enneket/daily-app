# 日报聚合展示网站开发计划

**日期**: 2026-02-11  
**需求**: 将 GitHub 仓库中的日报聚合展示为网站，并通过 GitHub Pages 部署

## 需求概述

开发一个静态网站，自动读取 GitHub 仓库中的日报 Markdown 文件，聚合展示为可浏览的网页，并通过 GitHub Pages 自动部署。

## 核心功能

### 1. 日报展示页面

#### 首页
- 显示最近 7 天的日报摘要
- 按日期倒序排列
- 支持快速跳转到具体日期

#### 日历视图
- 月历展示，标记有日报的日期
- 点击日期查看当天日报
- 支持月份切换

#### 列表视图
- 按时间线展示所有日报
- 支持分页或无限滚动
- 显示日期、标题、摘要

#### 详情页
- 完整显示某一天的日报内容
- Markdown 渲染
- 支持代码高亮
- 支持图片展示

### 2. 搜索和筛选

- 全文搜索
- 按日期范围筛选
- 按关键词筛选
- 按月份归档

### 3. 统计功能

- 总日报数量
- 连续打卡天数
- 本月/本年日报数
- 词云展示（可选）

## 技术方案

### 方案一：纯静态生成（推荐）

#### 技术栈
- **框架**: VitePress 或 VuePress
- **部署**: GitHub Pages
- **自动化**: GitHub Actions

#### 工作流程
```
提交日报到 GitHub
    ↓
GitHub Actions 触发
    ↓
读取所有日报 Markdown 文件
    ↓
生成静态网站
    ↓
部署到 GitHub Pages
```

#### 优点
- 完全静态，访问速度快
- 无需服务器，免费托管
- SEO 友好
- 自动化部署

### 方案二：动态加载

#### 技术栈
- **框架**: React + Vite
- **数据源**: GitHub API
- **部署**: GitHub Pages

#### 工作流程
```
访问网站
    ↓
通过 GitHub API 读取日报
    ↓
前端渲染展示
```

#### 优点
- 实时更新
- 无需构建步骤
- 灵活性高

#### 缺点
- 受 GitHub API 限制（每小时 60 次）
- 首次加载较慢
- SEO 不友好

## 推荐方案：VitePress + GitHub Actions

### 项目结构

```
daily-reports-site/
├── .github/
│   └── workflows/
│       └── deploy.yml          # 自动部署
├── docs/
│   ├── .vitepress/
│   │   ├── config.ts          # VitePress 配置
│   │   └── theme/
│   │       └── index.ts       # 自定义主题
│   ├── index.md               # 首页
│   ├── calendar.md            # 日历视图
│   ├── archive.md             # 归档页面
│   └── reports/               # 日报文件（自动同步）
│       └── 2026/
│           └── 02/
│               └── 11.md
├── scripts/
│   ├── sync-reports.js        # 同步日报脚本
│   └── generate-index.js      # 生成索引
└── package.json
```

## 开发阶段

### Phase 1: 基础框架搭建
- [ ] 初始化 VitePress 项目
- [ ] 配置 GitHub Pages 部署
- [ ] 创建基础页面结构
- [ ] 配置自定义主题

### Phase 2: 日报同步
- [ ] 编写脚本从日报仓库同步 Markdown 文件
- [ ] 生成日报索引和元数据
- [ ] 配置 GitHub Actions 自动同步

### Phase 3: 页面开发
- [ ] 首页：最近日报展示
- [ ] 日历视图：月历展示
- [ ] 列表视图：时间线展示
- [ ] 详情页：Markdown 渲染

### Phase 4: 功能增强
- [ ] 搜索功能
- [ ] 统计面板
- [ ] 标签系统
- [ ] RSS 订阅

### Phase 5: 优化和发布
- [ ] 性能优化
- [ ] SEO 优化
- [ ] 响应式设计
- [ ] 正式部署

## 实现细节

### 1. GitHub Actions 自动部署

```yaml
name: 部署日报网站

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 */6 * * *'  # 每 6 小时检查一次
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v4
      
      - name: 同步日报
        run: |
          # 从日报仓库拉取最新日报
          git clone https://github.com/用户名/daily-reports.git temp
          cp -r temp/2026 docs/reports/
      
      - name: 生成索引
        run: node scripts/generate-index.js
      
      - name: 构建网站
        run: |
          npm install
          npm run build
      
      - name: 部署到 GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/.vitepress/dist
```

### 2. 日报索引生成

```javascript
// scripts/generate-index.js
const fs = require('fs');
const path = require('path');

function generateIndex() {
  const reportsDir = 'docs/reports';
  const reports = [];
  
  // 遍历所有日报文件
  function walkDir(dir) {
    const files = fs.readdirSync(dir);
    files.forEach(file => {
      const filePath = path.join(dir, file);
      const stat = fs.statSync(filePath);
      
      if (stat.isDirectory()) {
        walkDir(filePath);
      } else if (file.endsWith('.md')) {
        // 提取日报信息
        const content = fs.readFileSync(filePath, 'utf-8');
        const date = extractDate(filePath);
        const title = extractTitle(content);
        const summary = extractSummary(content);
        
        reports.push({ date, title, summary, path: filePath });
      }
    });
  }
  
  walkDir(reportsDir);
  
  // 按日期排序
  reports.sort((a, b) => new Date(b.date) - new Date(a.date));
  
  // 生成索引文件
  fs.writeFileSync(
    'docs/.vitepress/reports-index.json',
    JSON.stringify(reports, null, 2)
  );
}

generateIndex();
```

### 3. VitePress 配置

```typescript
// docs/.vitepress/config.ts
import { defineConfig } from 'vitepress';

export default defineConfig({
  title: '我的日报',
  description: '每日工作记录',
  
  themeConfig: {
    nav: [
      { text: '首页', link: '/' },
      { text: '日历', link: '/calendar' },
      { text: '归档', link: '/archive' },
      { text: '统计', link: '/stats' }
    ],
    
    sidebar: {
      '/reports/': 'auto'
    },
    
    search: {
      provider: 'local'
    }
  },
  
  // 自定义 Markdown 配置
  markdown: {
    theme: 'github-dark',
    lineNumbers: true
  }
});
```

### 4. 首页展示

```vue
<!-- docs/index.md -->
---
layout: home

hero:
  name: 我的日报
  text: 记录每一天的成长
  tagline: 已坚持 XX 天
  actions:
    - theme: brand
      text: 查看最新
      link: /reports/latest
    - theme: alt
      text: 日历视图
      link: /calendar

features:
  - title: 📝 总日报数
    details: 120 篇
  - title: 🔥 连续打卡
    details: 30 天
  - title: 📊 本月日报
    details: 15 篇
---

<script setup>
import { data as reports } from './.vitepress/reports-index.data.js';

// 显示最近 7 天的日报
const recentReports = reports.slice(0, 7);
</script>

## 最近日报

<div v-for="report in recentReports" :key="report.date">
  <h3>{{ report.date }} - {{ report.title }}</h3>
  <p>{{ report.summary }}</p>
  <a :href="report.path">查看详情 →</a>
</div>
```

## 数据流程

### 日报提交流程
```
桌面应用提交日报
    ↓
推送到 GitHub 日报仓库
    ↓
触发网站仓库的 GitHub Actions
    ↓
同步日报文件
    ↓
重新生成索引
    ↓
构建静态网站
    ↓
部署到 GitHub Pages
```

### 访问流程
```
用户访问网站
    ↓
加载静态 HTML/CSS/JS
    ↓
读取日报索引
    ↓
渲染日报列表
    ↓
点击查看详情
    ↓
渲染 Markdown 内容
```

## 部署配置

### 1. 创建网站仓库
```bash
# 创建新仓库
gh repo create daily-reports-site --public

# 克隆到本地
git clone https://github.com/用户名/daily-reports-site.git
```

### 2. 配置 GitHub Pages
1. 进入仓库 Settings → Pages
2. Source 选择 "GitHub Actions"
3. 保存配置

### 3. 配置仓库访问
在日报仓库创建 Personal Access Token，用于网站仓库访问日报数据。

## 预计时间

- Phase 1: 0.5 天
- Phase 2: 1 天
- Phase 3: 1.5 天
- Phase 4: 1 天
- Phase 5: 0.5 天
- 总计：4-5 天

## 扩展功能（可选）

### 1. 评论系统
- 使用 Giscus（基于 GitHub Discussions）
- 支持对每篇日报评论

### 2. RSS 订阅
- 生成 RSS feed
- 支持订阅更新

### 3. 数据可视化
- 提交热力图（类似 GitHub Contributions）
- 词云展示
- 工作时长统计

### 4. 导出功能
- 导出为 PDF
- 导出为 Markdown 合集
- 导出为电子书（EPUB）

### 5. 主题切换
- 明暗主题
- 自定义配色

## 访问地址

部署后的网站地址：
```
https://用户名.github.io/daily-reports-site/
```

## 维护成本

- **服务器**: 免费（GitHub Pages）
- **域名**: 可选（可使用自定义域名）
- **维护**: 自动化，无需手动维护
- **存储**: GitHub 免费额度足够

## 总结

通过 VitePress + GitHub Actions 的方案，可以实现：
- ✅ 自动化部署
- ✅ 零成本托管
- ✅ 美观的展示界面
- ✅ 强大的搜索功能
- ✅ 完全静态，访问快速
- ✅ SEO 友好

这个方案既简单又强大，非常适合个人日报的展示和归档！
