# 修复窗口图标显示问题

## 问题描述
在开发模式和打包后的应用中，窗口标题栏和任务栏显示的是通用文档图标，而不是自定义的应用图标。

## 问题分析
1. 当前代码在 `main.ts` 中设置了 `icon` 参数，但路径可能不正确
2. 开发模式下图标路径与打包后的路径不同
3. 需要根据运行环境动态设置正确的图标路径

## 解决方案
1. 修正图标路径计算逻辑，区分开发模式和生产模式
2. 确保图标文件在正确的位置
3. 为不同平台设置正确的图标格式

## 实现步骤
1. 修改 `src/main/main.ts` 中的图标路径逻辑
2. 测试开发模式和打包后的图标显示
